function redraw()

end

function drawButton(name, x, y, width, height, text, color, activeColor, toggleAble, activedTime, OnClickedFucntion, OnInactiveFunction, isActivated)

end

function checkButton(buttonName)

end

function help()
	print("Usage : drawButton(name, x, y, width, height, text, color, activeColor, toggle-able, actived-time, OnClickedFucntion, OnInactiveFunction, Is-activated)")
	print("Value : drawButton(string, number, number, number, number, string, color, color, boolean, number, function, function, boolean)") 
end

help()